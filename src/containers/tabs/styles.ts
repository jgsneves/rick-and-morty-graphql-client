import styled from 'styled-components';
import Colors from '../../styles/colors';

const Wrapper = styled.div`
  display: flex;
  justify-content: center;
  border-bottom: 2px solid ${Colors.grayScale.gray2};
  margin: 20px auto;
`;

export default Wrapper;
